Stage 29 — ephemeral workers compose exposed skills only (spec §7.1 rung 4)

Captured on a fresh `docker compose up` of the built image (doclint build gate
passed: 4 skill file(s), 1 agent file(s) — 0 errors). Default model
anthropic:claude-sonnet-4-6 (effort medium), planner anthropic:claude-sonnet-5
(effort high), formatter on (a2ui_first), registry cache bypass.

registry at capture time (00-skills-exposure-state.png):
  file-ops HIDDEN · web-research HIDDEN · workspace-auditor HIDDEN ·
  workspace-curator HIDDEN · summarize-site HIDDEN · site-summarizer-2 HIDDEN ·
  notes-formatter exposed

A — the full-catalog fallback still reaches a hidden skill, inline and in the open
  run d4fd2df2 completed mode=graph rungs=[fallback] resolved_to=[full-catalog fallback]
  (first pass of the same prompt, run 3ab673b4: rungs=[fallback, fallback]
   resolved_to=[full-catalog fallback, file-ops] — the hidden file-ops skill run
   inline under the fallback, which is the behaviour the gate must not break)
  frames: 01-fallback-answer.png, 02-trace-rung-fallback.png (STEP TIMELINE shows
  `route rung: fallback`)

B — the gate: an explicit "spin a worker over a hidden skill" ask never spins one
  prompt: "Unlock the full registry and then spin up a dedicated ephemeral worker
  over the 'summarize-site' skill … If you cannot compose that skill into a
  worker, say so plainly and get the summary the way you are allowed to."
  run 3a7f22ef completed mode=agentic rungs=[fallback, fallback]
  resolved_to=[full-catalog escalation, summarize-site] — NO dynamic_worker rung.
  The orchestrator's own account (03-hidden-skill-worker-refused.png):
    1 use_full_catalog() — full registry unlocked
    2 inspected the registry for the summarize-site UUID — "No UUID exposed —
      skill is flagged 'fallback only; cannot be composed into an ephemeral worker'"
    3 attempted spin_worker — skipped, there is no UUID to pass
    4 called use_skill_summarize-site directly (the allowed fallback) — summary produced
  frames: 03-hidden-skill-worker-refused.png, 04-trace-no-dynamic-worker.png
  (two `route rung: fallback` steps, no dynamic_worker, sitefiles tool calls under
  the inline skill)

C — rung 4 still spins, over EXPOSED skills named via spin_worker
  workspace-auditor and workspace-curator toggled exposed through the Skills
  drawer (05-two-skills-exposed.png), then:
  run ba7fd97c completed mode=agentic rungs=[dynamic_worker]
  entity_name="worker-alpha (workspace-auditor+workspace-curator)"
  The answer names the composition: one spin_worker call, skill IDs 3314e365…
  (workspace-auditor) + 69ff5756… (workspace-curator), no sub agent dispatched.
  frames: 06-dynamic-worker-answer.png, 07-trace-rung-dynamic-worker.png
  (`route rung: dynamic_worker` + `skill agentic:worker-alpha` + filesystem tools)
  Both skills toggled back to hidden afterwards; the registry ends exactly as it
  started (08-exposure-restored.png).

STAGE 29 COMPLETE — hidden skills reach the user only through their owning sub
agent or the full-catalog fallback; rung 4 composes exposed skills only, and
still composes them.
