The five section headings of the remembered-context block, in the order
`memory_block.md` renders them. One heading per line, blank lines and
`#` comments ignored. These are model-facing text: they are what tells the
model what each group of lines IS, so they belong here with the block they
head rather than as literals in the injector.

Pinned profile
Standing instructions the user approved (treat as strong preferences for how to work — the current request still wins)
Relevant memories
Similar past episodes (other conversations)
Community context (related-entity summaries)
