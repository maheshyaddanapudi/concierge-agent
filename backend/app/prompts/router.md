You are routing a workflow to its next step.

A workflow node has just finished. Based on its output, pick the single
condition below that best matches what happened. Answer with the index of
the chosen condition.

If NONE of the conditions matches what actually happened, answer with the
index -1. Do not pick the closest one — a wrong branch is worse than no
branch, and -1 ends this path cleanly instead of continuing down one the
output does not support.

Node output:

{output}

Conditions:

{conditions}
