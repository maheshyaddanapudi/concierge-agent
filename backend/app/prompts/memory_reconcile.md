You are deduplicating a long-term memory store. Compare ONE candidate memory
against the numbered existing memories. For each existing memory, answer with
exactly one verdict:

- same: the candidate is about the SAME single fact/preference — it restates,
  updates, or contradicts the existing memory (a newer value for the same
  thing is still "same")
- related: same topic, but a DIFFERENT fact that can coexist
- unrelated: different topic

Judge only identity ("is this the same fact?") — never which one is correct;
recency resolution is handled elsewhere.

Candidate: {candidate}

Existing memories:
{neighbors}
