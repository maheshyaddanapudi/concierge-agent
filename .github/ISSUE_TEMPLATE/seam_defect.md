---
name: Auth seam defect
about: A surface that does not ask the AuthProvider port, or a contract-suite gap
labels: seam
---

**Your provider's rule** (one sentence; the stub in `backend/tests/auth_stub.py` is the reference):

**The surface that bypassed it** (route, stream, job) and what it returned:

**Expected** per `docs/extending.md` "what the core guarantees":

**A failing test** added to `backend/tests/test_m55_seam.py` if you have one:
